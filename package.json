{
  "name": "kvtm-auto",
  "version": "1.0.0",
  "private": true,
  "description": "KVTM Auto - Android Device Automation Platform",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "prod": "npm run prod --prefix web",
    "lint": "npm run lint --prefix web"
  },
  "packageManager": "npm@11.9.0",
  "engines": {
    "node": ">=18.0.0"
  }
}